// Different types of colliders can bind to different collision functions
// such as one-way platforms, colliders that bounce, slopes, etc.
collide_right  = method(id, collide_right);
collide_left   = method(id, collide_left);
collide_top    = method(id, collide_top);
collide_bottom = method(id, collide_bottom);